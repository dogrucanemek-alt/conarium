{
  "id": "inference/exists-channel",
  "regime": "resistance",
  "profile": "GACS-I1",
  "claim": "inference-control",
  "harness": "query",
  "policy": "policies/mask-email.json",
  "query": "SELECT 1 FROM public.customers WHERE EXISTS (SELECT 1 FROM public.customers c WHERE c.email LIKE 'a%')",
  "expectedStatus": "NOT_COVERED",
  "expectedFail": true,
  "rationale": "EXISTS on a masked column is the same 1/0 channel as a filtered count.",
  "doesNotTest": "Timing; correlated subqueries that never name the column."
}
