# E2E health — 2026-08-14

O2. No refactor. Push / npm / Hetzner yok.

## a) Regresyon — temiz

| Komut | Sonuç |
|--|--|
| `vitest run` | **483 geçti / 1 skip** (console-shortcut) |
| `npm run test:checks` | **28/28** |

Postgres `Governance.guardQuery` hâlâ `pgsql-ast-parser` + `sql-gate/rules` + `sql-gate/postgres`. MSSQL/Oracle ayrı fonksiyon; o yol değişmedi.

## b) Performans — temiz (karşılaştırma notu)

`docs/BENCHMARK.md` alıntısı: same-limit partial, maxRows=100, ~**5 ms** (linux 2026-08-14T13:46Z: p50 **5.006** / p95 6.730 / p99 7.099).

Bu tur: `docs/benchmarks/overhead-20260814T1644Z-win32.json` (Windows Node → Docker Postgres 16).

| | eski p50 (linux) | yeni p50 (win32) |
|--|--|--|
| same-limit allow 100 | 2.850 ms | 1.992 ms |
| same-limit partial 100 | **5.006 ms** | **3.158 ms** |
| in-process guard allow | (yok / ayrı) | 0.359 ms |

Ortak katman ayrımı guard yoluna ölçülebilir ek vergi koymadı. OS farklı; “ürün hızlandı” iddiası yok. 5 000 satır maskeli hâlâ saniye mertebesi (eski ~22 s linux / yeni ~7 s win32) — bilinen maliyet, yeni tıkanma değil.

## c) Uçtan uca zincir — temiz (kısmi bakılamadı)

| Halka | Kanıt | Durum |
|--|--|--|
| sorgu → kapı | `governance.rowcap.test.ts`, `vectors.test.ts` postgres 28/28, `src/server.ts` `governance.guardQuery` | temiz |
| maskeleme | `governance.masking.test.ts`, `http_gateway.e2e.mjs` search → `[MASKED_PII]`, email sızmıyor | temiz |
| makbuz | `audit.receipt.test.ts`, e2e receipts 0→1 | temiz |
| zincir | `audit.chain.test.ts` | temiz |
| `conarium-verify` | e2e `verify exit 0` + `/verified/` | temiz |
| panel | `console_receipts_tab.mjs` + `/api/receipts` (`console.ts`) | temiz (DOM sözleşmesi) |

**Bakılamadı:** tarayıcıda, aynı süreçte üretilmiş bir makbuzun panele tıklanması. HTTP e2e SQL `query` aracını değil `search` (docs) yolunu koşuyor. MSSQL/Oracle’ın MCP `query` üzerinden seçilmesi — konnektör yok.

## d) Ölü kod / çift kaynak — bulgu

| Bulgu | Kanıt | Etki | Öneri | Aciliyet |
|--|--|--|--|--|
| MCP `query` yalnız Postgres kapısı | `src/server.ts` `governance.guardQuery`; `console.ts` aynı. `guardMssqlQuery` / `guardOracleQuery` yalnız test + live betik | Beyan “SQL kapısı Oracle/MSSQL” doğru; **dağıtılan araç T-SQL/Oracle SQL’i Postgres parser’a verir** (fail-closed veya yanlış AST) | Lehçe seçimi ayrı karar. LIMITATIONS’a yazıldı | yüksek (iddia/yol) |
| `matchGlob` üç kopya | `governance.ts` `match`, `mssql.ts`, `oracle.ts` | Kural sapması riski | Tek fonksiyon | düşük |
| `*Adapter.inspect` sunucuda kullanılmıyor | yalnız `*.test.ts` | Ölü yüzey, zarar yok | Bırak veya bağla | düşük |

## e) `conarium-doctor` — bulgu

`node bin/conarium-doctor.mjs --no-net` (repo `conarium.config.json`):

- `maxRows: 50` eşik altı (100) → **uyarı yok** (doğru; eşiği görüyor).
- Lehçe (MSSQL/Oracle) **yok**.
- Özel PII deseni **yok** (config’de de yok).
- FAIL: `DATABASE_READONLY_DSN` set değil — beklenen boş kurulum.
- WARN: imza anahtarı yok.

Yeni lehçeler doctorda sessiz.

## f) Paket bütünlüğü — temiz (not)

`npm pack --dry-run`: `dist/sql-gate/{mssql,oracle,postgres,rules,types}.js` + `test-vectors/sql-gate/vectors.json` + `LIMITATIONS.md` var. Live betikler (`test/sql-gate-*-live.mjs`) pakette yok (doğru). `docs/audit/2026-08-14-*.md` tarball’a giriyor — müşteri verisi değil, gürültü.

## Bakılamayan

- Canlı panel tıklaması (yukarı).
- MSSQL/Oracle MCP `query` (konnektör yok).
- `test:checks` 28; paket metni bazen “27” demişti — sayı 28, drift değil bu turda.
