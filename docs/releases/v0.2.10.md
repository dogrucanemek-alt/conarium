# Draft — Conarium 0.2.10

**Taslak.** GitHub Release yayımlanmadı. Tag `v0.2.10` = `a21fdca`.
This is the npm / Hetzner / tag line as of 2026-08-14.

## Added

- `LIMITATIONS.md` and `LIMITATIONS.tr.md`. What this repository has not done.
- Built-in OpenTimestamps client. `javascript-opentimestamps` is not a
  dependency. The `web3` / `request` / `bitcore` tree is not installed.

## Changed

- `COMPARISON.md` removed from this repository. The dated comparison lives
  only at `conarium.dev/compare.html`.
- `test:checks` runs every check even if one fails.

## Docs

- Threat model and pentest scope under `docs/security/`.

Commits after this tag (benchmark, maxRows note) are not part of 0.2.10.

Not a dialect release. SQL remains Postgres only.
