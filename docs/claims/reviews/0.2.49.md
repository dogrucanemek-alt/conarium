# Claims review — 0.2.49

- base: v0.2.48
- head: 0a51b0c
- surface: sha256:46439a314e3f75c085c3df6e910adf0ac4f8d1dce42c0c2a9b9f9f33600dd382
- verdict: pass
- reviewer: Claude. No claim surface changed between v0.2.48 and this commit —
  the surface hash is byte-identical to the one recorded for 0.2.48, and the
  diff over the 24 surfaces is empty. The release carries version fields only
  (`package.json`, `server.json`, `package-lock.json`); no source, no prose.

It exists for one reason: the registry entry fixed in #91 — a display title and
`websiteUrl: https://conarium.dev`, neither of which the entry carried — cannot
reach the MCP registry under an already-published version number. The registry
refuses a duplicate version, so the correction needs a version of its own.

## Findings

none
